import styled from "styled-components";

const ListWrapper = styled.div`
  .tag-list {
    position: relative;
    z-index: 1;
    list-style: none;
    padding: none;
    display: flex;
    li {
      color: #fff;
      font-size: 16px;
      cursor: pointer;
      margin-right: 20px;
    }
  }
`;
export { ListWrapper };
